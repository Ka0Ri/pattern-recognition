import numpy as np
import matplotlib.pyplot as plt
from auxiliary import random_MixtureGaussian

# print("1.5.1")
# N = 500
# m = np.array([[1, 1], 
#         [3, 3]])
# S = np.array([[[0.1, 0.08], [0.08, 0.2]], 
#         [[0.1, 0], [0, 0.1]]])
# P = np.array([0.5, 0.5])
# X = random_MixtureGaussian(N, P, m, S)
# plt.axis('equal')
# plt.plot(X[:,0], X[:,1], 'o')
# plt.show()
# P = np.array([0.85, 0.15])
# X = random_MixtureGaussian(N, P, m, S)
# plt.axis('equal')
# plt.plot(X[:,0], X[:,1], 'o')
# plt.show()

